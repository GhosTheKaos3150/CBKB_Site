// Deletar Depois
export interface Todo {
  id: number;
  content: string;
}

export interface Meta {
  totalCount: number;
}

export interface Teacher {
  _id: string | undefined;
  name: string;
  desc: string;
}
// Deletar Depois
